import React from "react";
import './ChartInf';
import './Card.css';

const Chart = () => {
    const config = {
        type: 'bar',
        options: {
          scales: {
            y: {
              beginAtZero: true
            }
          }
        },
    };
}

export default Chart;